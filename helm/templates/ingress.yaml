
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: html-app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: html-app
            port:
              number: 80
